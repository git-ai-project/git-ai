//! CREATE + MODIFY + SUBMIT scenario buckets — generated by generate_matrix.py tooling.
//!
//! One #[test] per (family x observation x repeat) bucket. Each bucket runs
//! its scenarios through gt_sim::run_scenario and reports every violation in
//! a single panic, prefixed with stable scenario IDs for triage. See PLAN.md.

use super::gt_sim;
use super::scenario;

fn run_bucket(family: &str, observation: &str, repeat: &str) {
    let scenarios: Vec<_> = scenario::family(family)
        .into_iter()
        .filter(|s| s.observation == observation && s.repeat == repeat)
        .collect();
    assert!(
        !scenarios.is_empty(),
        "empty bucket {family}/{observation}/{repeat}"
    );
    let mut violations = Vec::new();
    for s in &scenarios {
        violations.extend(gt_sim::run_scenario(s));
    }
    assert!(
        violations.is_empty(),
        "{} attribution violations in {} {}/{} ({} scenarios):\n{}",
        violations.len(),
        family,
        observation,
        repeat,
        scenarios.len(),
        violations.join("\n")
    );
}

#[test]
fn create_blind_once() {
    run_bucket("CREATE", "blind", "once");
}

#[test]
fn create_traced_once() {
    run_bucket("CREATE", "traced", "once");
}

#[test]
fn modify_blind_once() {
    run_bucket("MODIFY", "blind", "once");
}

#[test]
fn modify_blind_twice() {
    run_bucket("MODIFY", "blind", "twice");
}

#[test]
fn modify_traced_once() {
    run_bucket("MODIFY", "traced", "once");
}

#[test]
fn modify_traced_twice() {
    run_bucket("MODIFY", "traced", "twice");
}

#[test]
fn submit_traced_once() {
    run_bucket("SUBMIT", "traced", "once");
}
